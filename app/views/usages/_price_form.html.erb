 <%= form_for(@usage) do |f| %>
  <% if @usage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usage.errors.count, "error") %> prohibited this test from being saved:</h2>

      <ul>
      <% @test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.fields_for :price do |builder| %>
    <div class="field">
      <%= builder.label "Elec Price" %><br />
      <%= builder.text_field :elec %>
    </div>
    <div class="field">
      <%= builder.label "Gas Price" %><br />
      <%= builder.text_field :gas %>
    </div>
    <div class="field">
      <%= builder.label "Biomass Price" %><br />
      <%= builder.text_field :biomass %>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>